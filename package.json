{
  "name": "smarthomekernel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build-ts",
    "build-ts": "tsc --build tsconfig.json && cpx ./lib/modules.js ./dist/lib",
    "clean": "rimraf ./dist",
    "serve": "npm run clean && npm run build && node dist/kernel/index.js",
    "start": "npm run serve",
    "watch-ts": "tsc --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/mqtt": "^2.5.0",
    "@types/node": "^14.17.5",
    "cpx": "^1.5.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "typescript": "^4.3.5"
  }
}
